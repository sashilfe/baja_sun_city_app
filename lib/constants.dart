import 'package:flutter/material.dart';

// Cores originais (para não quebrar o resto do app)
const primaryColor = Color(0xFFFFA500);
const secondaryColor = Colors.black12;
const bgColor = Colors.black87;

// Novas cores para um design mais moderno e técnico (Dark Theme)
const darkBgColor = Color(0xFF1D1D1D); // Um cinza bem escuro, quase preto
const darkSecondaryColor = Color(0xFF252526); // Um pouco mais claro para a sidebar
const darkDrawerColor = Color(0xFF19191A);
const orangeAccentColor = Colors.orangeAccent; // Cor de destaque vibrante
const darkCodeBgColor = Color(0xFF2D2D2D); // Fundo para blocos de código
const borderColor = Colors.white10;

const defaultPadding = 16.0;
